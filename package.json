{
  "name": "gh-pages",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pavel paiv Ivashkov",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^17.0.0",
    "uglify-js": "^3.0.0",
    "uglifyify": "^5.0.0"
  },
  "scripts": {
    "build": "make -C src clean all"
  }
}
